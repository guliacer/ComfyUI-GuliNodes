[project]
name = "ComfyUI-GuliNodes"
version = "1.0.1"
description = "GuliNodes - 尺寸适配 + 图像对比 + LoRA堆"
authors = [{ name = "Guli" }]
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/guliacer/ComfyUI-GuliNodes"

[tool.comfyui.custom_nodes]
"ComfyUI-GuliNodes" = { 
    "directory" = ".", 
    "web_directory" = "web" 
}